[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["cd engine && python3 -m pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "cd engine && python3 -m uvicorn api:app --host 0.0.0.0 --port ${PORT:-8000}"
